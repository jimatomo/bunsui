# bunsui job declaration (synced with `bunsui job sync`)
name: example_dbt
type: dbt
execution_mode: sync
depends_on: []
config:
  # build runs the model and its attached tests (run_results → assets)
  command: build
  select: example
  # dbt-only optional retries (default 0 = no retry; python jobs ignore these):
  # retries: 2
  # retry_delay_seconds: 2
