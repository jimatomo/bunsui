# bunsui job declaration (synced with `bunsui job sync`)
name: example_python
type: python
execution_mode: sync
depends_on:
- example_dbt  # `bunsui job run example_python` runs example_dbt first
config:
  callable: sample:main
