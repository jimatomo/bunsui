# Sample bunsui project

Generated by `uv run bunsui init`. Used for local smoke tests of the Hono API.

```bash
export BUNSUI_PROJECT="$(pwd)/examples/sample-project"
cd web && bun run dev:api
curl -s localhost:8787/api/status
```
